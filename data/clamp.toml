description = """
`Ord::clamp()` and `f32::clamp()` / `f64::clamp()` (stable since Rust 1.50) \
restrict a value to a range, replacing this crate.
"""
url = "https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.clamp"
